baseURL = 'https://sijeni.li.pona.la/'
title = 'sieni'
disableKinds = ["taxonomy", "term"]

[permalinks]
  tp = "/tp/:slug"
  sp = "/sp/:slug"

[outputs]
  home = ["HTML"]

[markup.goldmark.renderer]
  unsafe = true
[markup.goldmark.parser.attribute]
  block = true
  title = true

[caches]
  [caches.images]
    dir = ':cacheDir/images'
